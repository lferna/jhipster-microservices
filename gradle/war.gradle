apply plugin: "war"

bootWar {
    mainClassName = "io.github.jhipster.microservices.JhipstermicroservicesApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
